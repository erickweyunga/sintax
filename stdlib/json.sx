-- JSON standard library

fn (str s) parse:
    return __native_json_parse(s)

fn (value) str stringify:
    return __native_json_stringify(value)

fn (value) str pretty:
    return __native_json_pretty(value)
